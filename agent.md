# Agent Instructions

本文件中的规则适用于整个仓库及所有分支中的后续自动化编辑。

## GitHub Markdown 与数学公式

所有提交到 GitHub 的 Markdown 文档都必须保证在 GitHub **Preview** 中能够正常渲染。

1. 行内公式统一使用 `$...$`。
2. 块级公式统一使用独立成行的 `$$` 分隔符，或使用 `math` fenced code block：

   ````markdown
   $$
   y = f(x)
   $$
   ````

   或：

   ````markdown
   ```math
   y = f(x)
   ```
   ````

3. 不使用 `\(...\)` 或 `\[...\]` 作为 Markdown 数学公式分隔符；这些写法不作为本仓库的 GitHub Preview 兼容格式。
4. 块级公式前后保留空行，并避免将块级公式直接放进 Markdown 表格、普通代码块或行内反引号中。
5. 公式中若包含容易与 Markdown 冲突的字符，优先使用 GitHub 支持的 `$` + 反引号形式，或改用 `math` fenced code block。
6. 提交前必须检查本次修改涉及的全部 `.md` 文件，确认：
   - `$` / `$$` 成对闭合；
   - 不存在遗留的公式分隔符 `\(`、`\)`、`\[`、`\]`；
   - 公式没有被错误包裹在普通代码块中；
   - 标题、列表、表格和公式之间留有正确的空行。
7. 仅作为普通文本或代码示例出现的数学表达式可以使用反引号；需要排版显示的表达式必须使用上述 GitHub 数学语法。

## 修改原则

- 格式修复不得改变公式的数学含义、符号定义或实验结论。
- 修改已有文档时，应尽量保持正文、标题层级、链接和代码块不变，只调整影响 GitHub Preview 的格式。
